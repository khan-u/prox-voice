{
  "name": "prox-voice-node",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "PROX-VOICE browser node modules (proximity voice mesh, sensing, instrumentation)",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
